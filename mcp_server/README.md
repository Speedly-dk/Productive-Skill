# productive-mcp

Local stdio MCP server for [Productive.io](https://productive.io). Bundled with the [Productive Plugin for Claude Code](../README.md).

Standalone usage (outside the plugin):

```bash
uvx --from . productive-mcp
```

Requires `PRODUCTIVE_API_TOKEN` and `PRODUCTIVE_ORGANIZATION_ID` environment variables.
